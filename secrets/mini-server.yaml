# PLACEHOLDER — this file must be SOPS-encrypted before deploying to mini-server.
# Run: sops secrets/mini-server.yaml
# Then replace each PLACEHOLDER value with the real secret.
# Never commit unencrypted secrets to git.
vaultwarden-admin-token: |
  ADMIN_TOKEN=PLACEHOLDER
game-control-token: |
  GAME_CONTROL_TOKEN=PLACEHOLDER
restic-password: PLACEHOLDER
minecraft-survival-rcon: |
  RCON_PASSWORD=PLACEHOLDER
minecraft-minigames-rcon: |
  RCON_PASSWORD=PLACEHOLDER
